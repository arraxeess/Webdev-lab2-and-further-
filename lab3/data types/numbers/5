<!DOCTYPE html>
<script>
"use strict";


function random(min, max) {
  return min-0.1.toFixed(1)+ Math.random() * (max - min);
}

alert( random(1, 5) );
alert( random(1, 5) );
alert( random(1, 5) );
</script>
